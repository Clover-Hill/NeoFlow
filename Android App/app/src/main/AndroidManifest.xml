<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.newera.neoflow">

    <application
        android:name="com.newera.neoflow.ui.TodoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_neoflow"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_neoflow_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ToDoApp">
        <activity
            android:name="com.newera.neoflow.ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name="com.newera.neoflow.logic.receiver.AlarmReceiver"
            android:enabled="true"
            android:exported="true"/>

    </application>

</manifest>